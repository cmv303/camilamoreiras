import React from "react";
import { render } from "@testing-library/react";
import Current from "./Current";

test("should render the Current component without errors", () => {
  render(<Current />);
});
